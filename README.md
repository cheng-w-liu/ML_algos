ML_algos
========
```
  * src: contains implementations of selected machine learning algorithms, either in pure Python/Numpy, or in TensorFlow

  * test_notebooks: demos of the machine learning algorithms implemented in the `src` folder
```
